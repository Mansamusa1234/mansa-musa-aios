# Full fix scope

The code patch must locate the hourly audit implementation, normalize status/severity rendering, and harden social/video checks. Production provider tokens still require external re-authorization and deployment-secret updates after merge.
